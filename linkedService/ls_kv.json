{
	"name": "ls_kv",
	"properties": {
		"description": "Parameterised key vault linked service",
		"parameters": {
			"key_vault_URI": {
				"type": "string"
			}
		},
		"annotations": [
			"ls_kv"
		],
		"type": "AzureKeyVault",
		"typeProperties": {
			"baseUrl": "@{linkedService().key_vault_URI}"
		}
	}
}